name: expenses

services:
  expenses:
    image: expenses:latest
    container_name: expenses-dashboard
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "55007:80"
    env_file:
      - .env
    restart: unless-stopped
